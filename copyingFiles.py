import shutil 

#first import shutil module which has 3 main functions
    #copyfile() = copies the contents of a file
    #copy() = does what copyfile() AND copies the permissions mode - and its desitination can be a directory
    #copy2() = does everything copy() does AND copies metadata (the file's creation and modificatin times)

#copyfile(src,dst)
shutil.copyfile()